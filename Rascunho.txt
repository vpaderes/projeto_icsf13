    if(opcao == 1){

        for (int j=nC-1; j>=0; j--){
            for(int i=nL-1; i>=0; i--){
                ciclos++;
                if (matriz[i][j] == P){ //Verifica se é passageiro

                    *passageiros=*passageiros+1;
                    printf("\n\nPassageiros: %d\n\n", *passageiros);

                }
                if (matriz[i+1][j]== B) matriz[i+1][j] = V; // Muda a célula anterior da posição do onibus para vazio
                if (matriz[i][j] != '#') matriz[i][j] = B; // Verifica se for obstáculo, só move o ônibus se não for
                imprimeMapa(matriz, nL, nC);

                if (i==0 && j==0) { //Este código joga o ônibus para a outra extremidade do mapa, para caso ele chegue no final e ainda tenha passageiro para pegar na parte que ele não rodou
                    i = nL-1;
                    j = nC-1;
                    matriz[0][0] = V; // Muda a célula anterior da posição do onibus para vazio quando chega na última linha                    
                }
                if (ciclos==nL*nC){ //Verifica se o ônibus chegou na posição inicial do mapa - se já rodou tudo - e ativa uma flag para sair do loop
                    flag_para_looping = 1;
                }
            }
            if (matriz[0][j]== B) matriz[0][j] = V;
            
            if (flag_para_looping == 1) { // Muda o j para a condição de saída do loop depois de todos os usos dele, de modo que não bagunça a matriz
                j=-1;
            } 
        }
        printf("\n\nPassageiros: %d\n\n", *passageiros);
    }